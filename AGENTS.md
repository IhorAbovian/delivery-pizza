# AGENTS.md

## Overview

Next.js 15 (App Router) pizza delivery application.

## Commands

- `npm run dev` — start dev server
- `npm run build` — production build
- `npm run lint` — ESLint

## Architecture

- App Router (`app/`)
- TypeScript
- Tailwind CSS
- shadcn/ui components

## Notes

- `.agents/` directory is gitignored — contains AI agent skills only
- Branch: `junior`